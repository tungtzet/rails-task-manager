<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.completed ? '☑' : '☐' %> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<h4><%= link_to "+ add a new task", tasks_new_path %></h4>